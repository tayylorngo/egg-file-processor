# Backend environment variables.
# Copy to backend/.env for local development, or configure these directly in the
# Render dashboard for production.

# Comma-separated list of origins allowed by CORS. If unset, defaults to
# localhost:3000 and the production Netlify URL.
ALLOWED_ORIGINS=http://localhost:3000,https://eggfileprocessor.netlify.app
