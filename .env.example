# Frontend (Create React App reads variables prefixed with REACT_APP_).
# Copy to .env.local for local development. Production deploys (Netlify) should
# set this in the dashboard. If unset, the app falls back to the production URL.
REACT_APP_API_BASE_URL=http://127.0.0.1:8000
